# Como/onde o git guarda o histórico do projeto?

podemos usar o comando ``ls -al .git``, este comando serve para listar todos os arquivos do diretório ``.git``, lá estará todo o histórico do projeto.